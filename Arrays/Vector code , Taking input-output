#include<iostream>
#include<vector>
using namespace std;

int main(){
  int n;
  
  cout<<"Put the number"<<endl;
  cin>>n;
vector<int>arr(n);
  // initialize the Vector 1
  vector<int>brr(n,1);
  // Traverse Taking Input loop 
  for(int i = 0; i<n; i++){
    cin>>arr[i];
  }
  //Print Out input Loop
  for(int i = 0; i<n; i++){
    cout<<arr[i]<<" ";
  }
}
